{
  "name": "organization-service-e2e",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["organization-service"],
  "targets": {
    "e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{e2eProjectRoot}"],
      "options": {
        "jestConfig": "organization-service-e2e/jest.config.ts",
        "passWithNoTests": true
      },
      "dependsOn": ["organization-service:build", "organization-service:serve"]
    }
  }
}
